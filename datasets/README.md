# Datasets

Gesture/session datasets must carry session ID, timestamps, labels, raw IMU, filtered orientation, firmware version, config version, and sample rate. Large raw runs are not committed by default.
