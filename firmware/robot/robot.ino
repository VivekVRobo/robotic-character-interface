// PR-001 scaffold: host command -> firmware validation -> actuator layer.
// E-stop, heartbeat, TTL, packet validation, and joint limits are required
// before enabling real motion.
void setup() {}
void loop() {}
