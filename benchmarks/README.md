# Benchmarks

Each benchmark run records objective, hardware revision, firmware version, git commit, config hash, dataset/method, number of trials, raw results, summary metrics, limitations, and report.
