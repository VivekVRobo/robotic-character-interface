# Security and Safety Reporting

Do not publish exploit details for actuator-control bypasses, unsafe remote-control paths, or E-stop/watchdog bypasses before remediation. Report issues privately to the repository owner.

The dashboard is localhost-first. Remote actuator control is out of scope for V1 unless authenticated and explicitly enabled.
